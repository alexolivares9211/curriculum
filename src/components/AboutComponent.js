import React, {Component} from 'react';
import {Container} from "react-bootstrap";

class AboutComponent extends Component {
    render() {
        return (
            <Container fluid className="header-container" id="about">
                
            </Container>
        );
    }
}

export default AboutComponent;